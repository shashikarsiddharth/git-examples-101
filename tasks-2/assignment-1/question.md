### Question
Service Health Checker: Write a script that checks whether a list of services are running.

### Sample Output Exmaple:
$ bash ./service-checker.sh docker nginx ssh
nginx: STOPPED
docker: RUNNING
ssh: STOPPED