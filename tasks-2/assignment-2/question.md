### Question
Write a script that checks disk usage and alerts if any partition crosses a threshold.

### Sample Output Exmaple:
$ bash ./disk-usage.sh 80
Ex: If /dev is above 80%, print  
WARNING: /dev is 87% full
